def sendMessage(msg, emissor, time, clientSocket):
    
    print(f"\n{emissor}: {msg}. \n")
    
    #clientSocket.send(mensagem.encode())

    # -------------------------------
    # 8. ESPERA (SIMULA PROCESSAMENTO)
    # -------------------------------
    #time.sleep(2)